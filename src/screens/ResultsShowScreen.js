import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

const ResultsShowScreen = () => {
    return <View>
        <Text style={StyleSheet.text}> Results</Text>
    </View>
}

const styles=StyleSheet.create({
    text:{
        color: 'white'
    }
})

export default ResultsShowScreen
